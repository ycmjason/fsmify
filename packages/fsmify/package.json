{
  "name": "fsmify",
  "version": "0.0.1",
  "description": "A simple finite state machine library with first-class typescript support.",
  "main": "dist/main.js",
  "types": "src/main.ts",
  "repository": "https://github.com/ycmjason/fsmify",
  "author": "Jason Yu",
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "build": "esbuild src/main.ts --platform=node --bundle --minify --sourcemap --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "vitest": "^0.25.1"
  }
}
